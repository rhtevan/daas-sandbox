schema_version: 1

name: "org.kie.daas.system.packages"
version: "0.1"
description: "KIE DaaS system packages module"

# TODO: remove unnecessary package, (depending on image)
packages:
  install:
    - file
    - findutils
    - git
    - gzip
    - procps-ng
    - tar
    - unzip
    - vim-minimal
    - which
    - xz
